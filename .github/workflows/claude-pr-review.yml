name: Claude PR Review (Disabled)

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

jobs:
  review:
    name: Check PR Status
    runs-on: ubuntu-latest
    steps:
      - name: ℹ️ Claude PR Review Disabled
        run: |
          echo "Claude PR Review workflow is temporarily disabled"
          echo "Reason: anthropics/claude-code-action SDK exit code 1 error"
          echo ""
          echo "To validate your changes locally:"
          echo "  pnpm build    # Build Next.js"
          echo "  pnpm lint     # Run ESLint"
          echo "  pnpm type-check  # Run TypeScript"
          echo ""
          echo "This workflow can be re-enabled when:"
          echo "1. Upgrading to latest claude-code-action version"
          echo "2. Verifying ANTHROPIC_API_KEY secret configuration"
          echo "3. Testing MCP server connectivity"
